import { PageShell } from "@/components/PageShell";

export const metadata = { title: "AMI vs. Alfabetización Digital" };

export default function Page() {
  return (
    <PageShell title="AMI vs. Alfabetización Digital" kicker="Contenido">
      <p className="text-sm text-[var(--muted)]">
        En producción esta URL responve con experiencia propia (posible contenido
        dinámico o incrustado). Portar el recurso completo desde Wix sin pérdida de
        interacción.
      </p>
    </PageShell>
  );
}
